// import type { NextConfig } from "next";

const nextConfig = {
  transpilePackages: ['@vds/components'
  //  , '@vds/core', '@tn/foundation'
  ],
  /* config options here */
  reactStrictMode: true,
};

export default nextConfig;
